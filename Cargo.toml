[package]
name = "rc-writer"
version = "1.0.0"
authors = ["Magic Len <len@magiclen.org>"]
repository = "https://github.com/magiclen/rc-writer"
homepage = "https://magiclen.org/rc-writer"
keywords = ["rc", "rcmut", "refcell", "write", "mutex"]
description= "A tiny implement for writing data to a reference counted instance."
readme = "README.md"
license = "MIT"

[badges]
travis-ci = { repository = "magiclen/rc-writer", branch = "master" }
appveyor = { repository = "magiclen/rc-writer", branch = "master", service = "github" }

[dependencies]
